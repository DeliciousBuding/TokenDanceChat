# TokenDanceChat Security Risk Register

Last reviewed: 2026-07-31

TokenDanceChat is a demo and AgentHub stack validation app. The risk target is to keep it safe as a public demo while avoiding accidental drift into production identity or messaging infrastructure.

## Scope

- Backend REST/WebSocket/OIDC/media/webhook: `backend/`
- Frontend/PWA/service worker: `frontend/`
- Deployment and docs: `Dockerfile*`, `docker-compose.yml`, `docs/`, `README.md`, `SECURITY.md`

## P0 / High

| ID | Severity | Status | Risk | Evidence | Next action |
|---|---|---:|---|---|---|
| CHAT-SR-001 | High | Mitigated in repo; verify in deployment | WebSocket identity for registered local users is now bound to a server-issued app session token, while OIDC-linked usernames still require OIDC token validation and unknown usernames remain guest-only. | `backend/hub/client.go:324`, `backend/hub/hub.go:523`, `backend/handler/handler.go:86`, `backend/handler/handler.go:161`, `backend/store/store.go:2875`, `frontend/src/hooks/useWebSocket.ts:142`, `frontend/src/components/AuthModal.tsx:57`, `backend/hub/hub_test.go:504`, `backend/store/store_test.go:698`, `frontend/src/hooks/useWebSocket.test.ts:293` | Verify deployed `CHAT_SESSION_SECRET` stability, login/register WS join, guest join, and OIDC join in the target environment before closing. |
| CHAT-SR-002 | High | Mitigated in repo; verify in deployment | Authenticated search is now caller-aware. `/api/search` passes the authenticated session username to the store; the store filters deleted messages and limits global results to public messages, the caller's DMs, and groups where the caller is a member. | `backend/handler/handler.go:411`, `backend/hub/hub.go:70`, `backend/store/store.go:1023`, `backend/store/store.go:1027`, `backend/store/store_test.go:326`, `backend/handler/handler_test.go:1834` | Verify deployed search behavior against real public, DM, group, non-member group, and deleted-message fixtures before closing. |
| CHAT-SR-016 | High | Mitigated in repo; verify preview policy | `/api/messages` still supports unauthenticated public preview, but anonymous callers are capped to the latest 20 public messages and cannot use `before` pagination. Supplying `before` without a valid app session returns `401 AUTH_REQUIRED`; authenticated callers keep the existing bounded `limit`/`before` history path. | `backend/handler/handler.go:72`, `backend/handler/handler.go:203`, `backend/handler/handler.go:347`, `backend/handler/handler.go:353`, `backend/handler/handler.go:359`, `backend/handler/handler_test.go:2209`, `backend/handler/handler_test.go:2499`, `backend/handler/handler_test.go:2520`, `backend/handler/handler_test.go:2588` | Verify deployed public preview returns only the latest bounded sample, unauthenticated `before` returns `AUTH_REQUIRED`, and authenticated pagination still works. Decide whether 20 messages is the intended public sample size. |
| CHAT-SR-017 | High | Mitigated in repo; verify in deployment | Group export now requires the authenticated caller to be a member of the requested group before `ExportMessages` is called. Non-members receive `403 NOT_IN_GROUP`. | `backend/handler/handler.go:983`, `backend/handler/handler_test.go:3146`, `backend/handler/handler_test.go:3167`, `backend/store/store.go:2104` | Verify deployed group export behavior with member and non-member fixtures before closing. |
| CHAT-SR-021 | High | Mitigated in repo; verify in deployment | Room-scoped WebSocket chat messages previously used global Hub broadcast, so a user connected in another room could receive message bodies and `@all` notifications outside their current room. `handleChatMessage` now snapshots the current room and fanouts message, `mention_all`, mention notifications, and assistant-unconfigured system feedback only to clients in that room. | `backend/hub/client.go:545`, `backend/hub/client.go:563`, `backend/hub/client.go:578`, `backend/hub/client.go:581`, `backend/hub/client.go:605`, `backend/hub/hub.go:961`, `backend/hub/hub_test.go:3154` | Verify deployed room switching with two users in different rooms, including normal messages and `@all`, before closing. |
| CHAT-SR-003 | High | Partially mitigated; role policy open | Invite generation/listing now derives the creator from the authenticated session, but there is still no admin/role policy around who can mint invite codes. Any registered session can create invites unless product policy explicitly allows that. | `backend/handler/handler.go:1218`, `backend/handler/handler.go:1244`, `backend/handler/handler.go:1337`, `backend/handler/handler.go:1342`, `backend/handler/handler_test.go:2830` | Define invite ownership policy; add role checks or explicit self-service limits and tests. |
| CHAT-SR-004 | High | Partially mitigated; quota/retention open | Uploads now require a session and have request-size caps, but still act as authenticated public file hosting without per-user quotas, ownership checks on served files, or lifecycle cleanup. | `backend/handler/handler.go:578`, `backend/handler/handler.go:584`, `backend/handler/handler.go:589`, `backend/handler/handler.go:667`, `backend/handler/handler.go:673` | Add per-user quotas, lifecycle cleanup, served-file ownership/visibility rules, and release checks for upload disk growth. |
| CHAT-SR-005 | High | Mitigated in repo; verify in deployment | Webhook ingress now requires `Authorization: Bearer <secret>`, rejects query-string secrets, applies an 8 KiB body cap, rejects content over 2000 characters, persists the accepted message, and sends it only through group-member fanout. | `backend/handler/handler.go:1282`, `backend/handler/handler.go:1299`, `backend/handler/handler.go:1317`, `backend/handler/handler.go:1321`, `backend/handler/handler.go:1342`, `backend/handler/handler_test.go:428`, `backend/handler/handler_test.go:456`, `backend/handler/handler_test.go:486`, `docs/webhook-integration.md:19` | Verify deployed webhook clients have switched to header auth and confirm reverse-proxy logs do not contain webhook secrets before closing. |

## P1 / Medium

| ID | Severity | Status | Risk | Evidence | Next action |
|---|---|---:|---|---|---|
| CHAT-SR-006 | Medium | Mitigated in repo; verify in deployment | OIDC provider calls have a 5s HTTP timeout and response-size caps. OIDC state/redeem token stores have hard capacity and reject new entries at capacity instead of evicting in-flight logins; their cleanup loops are closable, failed `SetupOIDC` does not install transient stores, and reconfiguration closes previous stores. OIDC endpoints have an independent per-IP rate limit, and trusted proxy parsing avoids all users sharing the reverse proxy `RemoteAddr` while ignoring spoofed `X-Forwarded-For` prefixes. Rate limiter buckets prune expired IP entries. | `backend/handler/oidc.go:31`, `backend/handler/oidc.go:32`, `backend/handler/oidc.go:85`, `backend/handler/oidc.go:108`, `backend/handler/oidc.go:166`, `backend/handler/oidc.go:189`, `backend/handler/oidc.go:324`, `backend/handler/oidc.go:363`, `backend/handler/oidc.go:455`, `backend/handler/oidc.go:541`, `backend/handler/ratelimit.go:37`, `backend/handler/ratelimit.go:147`, `backend/handler/ratelimit_test.go:216`, `backend/handler/ratelimit_test.go:228`, `backend/handler/oidc_test.go:59`, `backend/handler/oidc_test.go:76`, `backend/handler/oidc_test.go:163`, `backend/handler/oidc_test.go:179` | Verify deployed `CHAT_TRUSTED_PROXY_CIDRS` matches the actual reverse proxy IP/CIDR, then exercise multiple OIDC login flows from distinct client IPs behind the proxy. |
| CHAT-SR-007 | Medium | Mitigated in repo; verify in deployment | Link preview SSRF defenses now validate both the pre-fetch hostname and the final IP selected by the HTTP transport. The custom `DialContext` rejects loopback, link-local, private, unspecified, unresolved, and empty address results immediately before dialing, then connects to the verified IP instead of allowing a second resolver lookup. Redirects remain HTTPS-only and private-host blocked. | `backend/handler/handler.go:524`, `backend/handler/handler.go:963`, `backend/handler/handler.go:970`, `backend/handler/handler.go:975`, `backend/handler/handler.go:979`, `backend/handler/handler.go:996`, `backend/handler/handler_test.go:2278` | Verify deployed `/api/link-preview` still fetches normal public HTTPS pages and rejects localhost/private/link-local targets, including redirect and DNS-rebinding-style resolver cases. |
| CHAT-SR-008 | Medium | Open | Group call room IDs function as bearer capability. Anyone with or guessing a room ID can join/list/relay SDP or ICE. | `backend/hub/client.go:2837`, `backend/hub/client.go:3058`, `backend/hub/client.go:3122`, `backend/hub/client.go:3171` | Enforce participant allowlists on join/list/signaling, use invite tokens/TTL, and cap participants. |
| CHAT-SR-009 | Medium | Mitigated in repo; verify in deployment | Service worker no longer caches `/api/*`: API GET requests use direct `fetch(event.request)` and never enter the `networkFirst` path that writes to Cache Storage. Static assets still use stale-while-revalidate, and the cache name was bumped to evict old API-bearing cache entries. | `frontend/public/sw.js:1`, `frontend/public/sw.js:32`, `frontend/public/sw.js:68`, `frontend/public/sw.js:70`, `frontend/public/sw.js:84`, `frontend/public/sw.js:89`, `frontend/public/sw.js:105` | Verify a deployed browser profile upgrades to `tdchat-v5`, old caches are removed, and `/api/*` entries are absent from Cache Storage. |
| CHAT-SR-010 | Medium | Mitigated in repo; verify in deployment | CORS and WebSocket origin checks now share one explicit origin allowlist. Same-origin requests are host-exact; cross-origin requests require `CHAT_ALLOWED_ORIGINS` entries with scheme, such as `https://chat.example.com` or `https://*.example.com`. Bare hostnames, `.example.com`, `*`, and subdomain origins with unexpected ports are not allowed. | `backend/handler/origin.go:21`, `backend/handler/origin.go:50`, `backend/handler/handler.go:255`, `backend/handler/ws.go:15`, `backend/handler/ws_test.go:80`, `backend/handler/ws_test.go:156`, `backend/main_config_test.go:291`, `backend/main_config_test.go:336`, `.env.example:4` | Verify deployed `CHAT_ALLOWED_ORIGINS` uses full origins for the actual browser host(s), including port where present, and confirm browser CORS/WebSocket connection from the production origin. |
| CHAT-SR-011 | Medium | Mitigated in repo; verify in deployment | Initial OIDC login now requires the redeemed username, access token, and app `session_token`, while treating refresh tokens as optional. This matches Chat's current `openid profile email` request and memory-only refresh model, so providers can omit refresh without preventing the authenticated WS join. | `backend/handler/oidc.go`, `frontend/src/App.tsx`, `frontend/src/App.test.tsx`, `frontend/src/stores/chatStore.ts`, `docs/oidc-setup.md` | Deploy and repeat a real provider login that omits refresh; verify the authenticated username, non-guest state, and connected WebSocket before closing. |
| CHAT-SR-012 | Medium | Verify | OIDC access/refresh tokens are currently held in Zustand memory, while localStorage keeps app auth markers and username. Future refresh persistence would change the threat model. | `README.md:172`, `docs/oidc-setup.md:70`, `frontend/src/stores/chatStore.ts:230`, `frontend/src/stores/chatStore.ts:369` | Keep refresh tokens memory-only unless switching to an HttpOnly app session; add regression that OIDC tokens are not written to localStorage. |
| CHAT-SR-013 | Medium | Verify | CSP allows `ws:` and `wss:` in `connect-src`, and inline styles remain allowed. The policy should be validated at runtime because meta CSP and server headers can diverge. | `frontend/index.html:9`, `SECURITY.md:59`, `SECURITY.md:23` | Keep runtime header checks in release validation and tighten `connect-src` when deployment stabilizes. |
| CHAT-SR-014 | Low | Open | Service worker cache strategy has been fixed before, but stale assets remain a recurring release risk for a PWA demo. | `AGENTS.md:73`, `AGENTS.md:211`, `ROADMAP.md:56`, `docs/handoff-report-2026-05-25.md:199` | Keep cache version changes in release checklist and verify HTML references the latest JS hash after deploy. |
| CHAT-SR-015 | Low | Partially mitigated; broader doc sweep open | The 2026-05-25 handoff report has been sanitized to remove private host aliases, internal IPs, real deployment domains, host ports, container paths, and SSH commands. Some older docs/scripts may still contain local paths or deployment examples that should be reviewed before public packaging. | `docs/handoff-report-2026-05-25.md:57`, `docs/visual-acceptance.md:60`, `deploy.md:22`, `scripts/deploy.sh:22` | Continue replacing personal/local paths with workspace-relative examples and keep live deployment commands in the private operations workspace. |
| CHAT-SR-018 | Medium | Open | `/api/admin/stats` now requires a session, but has no admin role check. Any authenticated user can read operational counts and registered-user totals. | `backend/handler/handler.go:1309`, `backend/handler/handler.go:1315`, `backend/handler/handler.go:1318` | Add an admin/maintainer authorization concept before exposing admin endpoints beyond local demo use. |
| CHAT-SR-019 | Medium | Mitigated in repo; verify in deployment | Webhook ingress ignores request-body sender names and broadcasts with the server-derived `webhook` display name. | `backend/handler/handler.go:1332`, `backend/handler/handler_test.go:516`, `frontend/src/e2e/webhook-ingress.test.ts:71`, `frontend/src/e2e/webhook-ingress.test.ts:80` | Verify deployed webhook messages display as `webhook` and not as caller-supplied usernames before closing. |
| CHAT-SR-020 | Medium | Open | Webhook ingress has no visible per-webhook rate limit. A valid webhook secret can spam group broadcasts and consume hub/store resources. | `backend/handler/handler.go:1259`, `backend/handler/handler.go:1276`, `backend/handler/handler.go:1305` | Add per-webhook and per-IP rate limits plus audit counters before broadcast. |

## Recent Mitigation Evidence

- 2026-05-25: `CHAT-SR-001` was mitigated in repo by adding local session tokens on login/register, registering the HTTP handler as the Hub's session verifier, checking `Store.UserExists` during WS join, and sending the session token from login/register UI paths.
- 2026-05-25: `CHAT-SR-002` was mitigated in repo by adding `SearchMessagesForUser`, wiring `/api/search` to the authenticated session username, filtering deleted messages in search, and adding store/handler regression coverage for private/deleted-result leakage.
- 2026-05-25: `CHAT-SR-017` was mitigated in repo by checking group membership in `/api/export?conversation=group:<name>` before store export and adding regression coverage for both non-member denial and member export.
- 2026-05-25: `CHAT-SR-005` and `CHAT-SR-019` were mitigated in repo by moving webhook secrets out of URLs into `Authorization: Bearer`, adding body/content limits, rejecting query-string secrets, persisting accepted webhook messages with real group message IDs, using group-member fanout, and using a server-derived webhook sender.
- 2026-05-25: `CHAT-SR-006` was mitigated in repo by replacing direct OIDC provider calls with a bounded `http.Client`, adding response body caps, adding capacity/backpressure and cleanup lifecycle handling to OIDC state/redeem token stores, adding OIDC endpoint rate limits, parsing trusted reverse-proxy client IPs, and pruning expired rate-limit IP entries.
- 2026-05-25: `CHAT-SR-015` was partially mitigated by sanitizing `docs/handoff-report-2026-05-25.md`, README/deploy examples, and OIDC compose examples so they record deployment contracts and local/placeholder verification commands instead of private topology details.
- 2026-05-25: `CHAT-SR-009` was mitigated in repo by making Service Worker `/api/*` requests network-only and bumping `CACHE_NAME` to `tdchat-v5`, preventing authenticated REST responses from being persisted in Cache Storage.
- 2026-05-25: `CHAT-SR-021` was mitigated in repo by replacing global chat-message broadcast with room-scoped fanout and adding a regression that a client in `room-2` does not receive a `room-1` message or `mention_all`.
- 2026-05-25: `CHAT-SR-016` was mitigated in repo by preserving unauthenticated public preview as a bounded latest-message sample, capping anonymous `limit` to 20, requiring an app session for `before` pagination, and keeping authenticated pagination behavior covered by handler regressions.
- 2026-05-25: `CHAT-SR-010` was mitigated in repo by moving CORS and WebSocket origin checks onto a shared parser, requiring explicit scheme-bearing allowlist entries, rejecting `*` for cross-origin requests, and adding regressions for host/port mismatches, wildcard subdomains, bare domains, and unexpected ports.
- 2026-05-25: `CHAT-SR-007` was mitigated in repo by giving link preview fetches a guarded `DialContext` that validates the final resolved IP immediately before TCP connect and refuses DNS-rebinding-style private address pivots before the underlying dialer runs.
- Fresh focused checks passed:
  - `cd backend; go test ./hub -run "TestHandleJoin(RequiresSessionTokenForRegisteredUser|RejectsWrongSessionTokenForRegisteredUser|AcceptsSessionTokenForRegisteredUser|AllowsGuestWithoutToken|RequiresTokenForOIDCUser)" -count=1`
  - `cd backend; go test ./handler -run "TestVerifySessionJoinToken|TestProtectedRESTRequiresSession|Test(Login|Register).*Session|Test.*OIDC" -count=1`
  - `cd backend; go test ./store -run "Test(UserExists|RegisterUser|VerifyUserBcryptUpgradeFlow)" -count=1`
  - `cd backend; go test ./store -run "TestSearchMessages(ForUserFiltersPrivateAndDeletedResults|SpecialCharacters|EmptyQuery|SQLInjection|NonExistentRoom|EmptyResults|Limit|VeryLongQuery|CJKAndEmoji)?$" -count=1`
  - `cd backend; go test ./handler -run "Test(SearchMissingQuery|SearchWrongMethod|SearchUsesAuthenticatedUserScope|ProtectedRESTRequiresSession)$" -count=1`
  - `cd backend; go test ./handler -run "TestExportMessages(GroupRequiresMembership|GroupAllowsMember|DMUsesSessionUsername|JSON|Text|InvalidFormat|WrongMethod|VeryLargeLimit|WithinLimit|ZeroLimit)$" -count=1`
  - `cd backend; go test ./handler -run "TestWebhookHandler(VerifiesHashedSecret|RejectsQuerySecret|RejectsOversizedBody|RejectsOversizedContent|UsesServerDerivedSender)$" -count=1`
  - `cd backend; go test ./handler -run "Test(OIDCHTTPClientHasBoundedTimeout|ReadOIDCResponseBodyRejectsOversizedResponse|OIDCConfigHandler|OIDCLoginRedirect|OIDCCallbackSuccess|OIDCRefresh|OIDCExchange|VerifyOIDCJoinToken)$" -count=1`
  - `cd backend; go test ./handler -run "Test(OIDCStateStoreCloseStopsCleanupLoop|OIDCTokenStoreCloseStopsCleanupLoop|SetupOIDCFailureDoesNotInstallTransientStores|SetupOIDCReconfigureClosesPreviousTransientStores|OIDCStateStoreRejectsNewEntriesAtCapacity|OIDCTokenStoreRejectsNewEntriesAtCapacity|RequestIPUsesForwardedForFromTrustedProxy|RequestIPIgnoresSpoofedForwardedForPrefix|RequestIPIgnoresForwardedForFromUntrustedRemote|OIDCAllowBudgetsFourCompleteRedirectFlows|RateLimiterPrunesExpiredIPEntries|OIDCLoginRateLimitedByIP|OIDCRefreshWrongMethodDoesNotConsumeOIDCRateLimit)$" -count=1`
  - `cd backend; go test ./handler -run "Test(OIDC|RateLimit|AuthAllow|WSAllow)" -count=5 -shuffle=on`
  - `cd backend; go test ./handler -run "TestGetMessages|TestProtectedRESTRequiresSession" -count=1 -v`
  - `cd backend; go test ./handler -run "Test(CORS|COR|CheckOrigin|WebSocketUpgrade)" -count=1 -v`
  - `cd backend; go test ./handler -run "Test(LinkPreviewDialContextRejectsPrivateResolvedIP|LinkPreview|IsPrivateHost)" -count=1 -v`
  - `cd backend; go test . -run "TestCORSMiddleware" -count=1 -v`
  - `cd backend; go test ./hub -run "Test(BroadcastToRoom|HandleChatMessageScopesRoomFanout)$" -count=1 -v`
  - `node --check frontend\public\sw.js`
  - `cd frontend; pnpm test -- --run src/stores/chatStore.test.ts src/hooks/useWebSocket.test.ts src/lib/api.test.ts`
  - Historical frontend webhook management UI has been removed from the lightweight contract; webhook security coverage remains in backend handler/hub/store tests.
  - `.\scripts\verify.ps1 -SkipDocker`

## Verification Queue

Run these from `D:\Code\TokenDance\tokendance-chat`:

```powershell
cd backend; go test ./handler ./hub ./store
cd ..\frontend; npm test -- --run src/App.test.tsx src/hooks/useWebSocket.test.ts src/lib/api.test.ts
rg -n "GetOIDCUserByUsername|msg.Token|setItem\(\"tokendance:auth\"|connect\(name" backend frontend
rg -n "GetMessages|Search|ExportMessages|AdminStats|InviteGenerate|InviteList" backend/handler
rg -n "UploadImage|ServeUpload|WebhookHandler|LinkPreview|isPrivateHost|OIDCLogin|http.Get|http.PostForm|io.ReadAll\(resp.Body\)" backend/handler
rg -n "call_room|call_ice_candidate|CHAT_ALLOWED_ORIGINS|cache.put|isApiRequest" backend frontend .env.example
```

## Loop Notes

- Keep `SECURITY.md` for public summary and this file for the active audit queue.
- If Chat grows beyond demo scope, revisit identity/session, export/search authz, media retention, and WebSocket origin policy together.
